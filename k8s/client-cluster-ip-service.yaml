apiVersion: v1
kind: Service
metadata:
  name: client-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: web
  ports:
    # This is the port at which ClusterIP object will be listening
    - port: 3000
      # This is the port on which actual Pod will be listening
      targetPort: 3000
